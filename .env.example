# Environment Configuration for AI-Driven Cybersecurity Platform

# Database Password (REQUIRED for Docker deployment)
# Generate a strong password: openssl rand -base64 32
DB_PASSWORD=your_secure_password_here

# Optional: Model path
# MODEL_PATH=/app/models/trained/RandomForest_model.pkl

# Optional: API Configuration
# API_HOST=0.0.0.0
# API_PORT=5000

# Optional: Dashboard Configuration
# DASHBOARD_HOST=0.0.0.0
# DASHBOARD_PORT=8000

# Optional: Database Connection (for local development)
# DATABASE_URL=postgresql://cybersec_user:password@localhost:5432/cyber_threat_db
